---
- hosts: linecards
  become: yes
  tasks:
  roles:
    - network_common
    - linecard_common
    - network_netq
  handlers:
    - include: handlers/main.yml

- hosts: fabriccards
  become: yes
  roles:
    - network_common
    - fabriccard_common
    - network_netq
  handlers:
    - include: handlers/main.yml

- hosts: leafs
  become: yes
  roles:
    - network_common
    - leaf_common
    - network_netq
  handlers:
    - include: handlers/main.yml
